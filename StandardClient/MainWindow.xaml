<Window x:Class="StandardClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Standard Client" Height="250" Width="400"
        Background="#f5f6fa" Closed="Window_Closed">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CLIENT ỨNG DỤNG (REAL METRICS)" FontSize="18" FontWeight="Bold" Foreground="#2f3640" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="BtnConnect" Content="Kết Nối Server" Width="120" Height="35" Margin="0,0,10,0" Background="#4cd137" Foreground="White" FontWeight="Bold" Click="BtnConnect_Click"/>
            <Button x:Name="BtnDisconnect" Content="Ngắt Kết Nối" Width="120" Height="35" Background="#e84118" Foreground="White" FontWeight="Bold" Click="BtnDisconnect_Click" IsEnabled="False"/>
        </StackPanel>

        <StackPanel Grid.Row="2" VerticalAlignment="Center">
            <TextBlock x:Name="TxtSpeed" Text="Download: 0 KB/s | Upload: 0 KB/s" HorizontalAlignment="Center" FontSize="14" FontWeight="Bold" Foreground="#0097e6" Margin="0,0,0,10"/>
            <TextBlock x:Name="TxtStatus" Text="Trạng thái: Chưa kết nối" Foreground="#718093" FontStyle="Italic" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>